#include "HashTable.h"
#include <iostream>

//Texture2D*& HashTable::operator[](const char* key)
//{
//    auto hashedKey = Hash(key, strlen(key)) % tableSize;
//    return hashTable[hashedKey];
//}
//unsigned int HashTable::Hash(const char* data, unsigned int size)
//{
//    unsigned int hash = 0;
//    for (unsigned int i = 0; i < size; i++) {
//        hash += data[i];
//    }
//    return hash;
//}
